name: Dependency Updates

on:
  schedule:
    - cron: "0 12 * * 1" # Every Monday noon
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check for outdated dependencies
        run: |
          npm install -g npm-check-updates
          ncu -u
          npm install
